{
  "identifier": "default",
  "description": "Default capabilities for the main window",
  "windows": ["main", "tray-popover"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "drag:default"
  ]
}
